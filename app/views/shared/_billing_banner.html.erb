<% if subscription_restricted? %>
  <div class="flex items-center justify-between gap-3 bg-red-600 px-4 py-2 text-sm font-medium text-white">
    <span><%= t("billing.restricted_mode.banner") %></span>
    <%= link_to t("billing.restricted_mode.cta"), settings_billing_path,
                class: "rounded-md bg-white/20 px-3 py-1.5 font-semibold hover:bg-white/30" %>
  </div>
<% end %>
