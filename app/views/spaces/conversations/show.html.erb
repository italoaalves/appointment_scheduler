<div class="flex h-[calc(100dvh-var(--spacing-inbox-mobile-chrome))] min-h-0 min-w-0 w-full max-w-full flex-col overflow-x-clip overflow-y-hidden sm:h-[calc(100dvh-var(--spacing-inbox-desktop-chrome))]"
     data-controller="inbox">

  <%= render "spaces/conversations/detail_frame",
             conversation: @conversation,
             standalone: true,
             channel: @channel,
             can_write_inbox: @can_write_inbox %>

</div>
