<%= turbo_frame_tag "conversation_detail", class: "flex h-full min-h-0 min-w-0 w-full max-w-full flex-1 flex-col overflow-x-hidden overflow-y-hidden" do %>
  <%= render "spaces/conversations/detail",
             conversation: conversation,
             standalone: standalone,
             channel: channel,
             can_write_inbox: can_write_inbox %>
<% end %>
