<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || t("layout.app_name") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets (can layer on top of Tailwind) %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-slate-50 text-slate-900">
    <% if user_signed_in? %>
      <nav x-data="{ open: false }" class="bg-slate-900 border-b border-slate-800">
        <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-14 items-center justify-between">
            <div class="flex items-center gap-4">
              <%= link_to t("layout.app_name"), root_path, class: "text-sm font-semibold text-white tracking-tight" %>
              <div class="hidden md:flex items-center gap-3 text-sm">
                <% if current_user.admin? %>
                  <%= link_to t("layout.nav.all_appointments"), admin_appointments_path, class: "text-slate-200 hover:text-white px-2 py-1 rounded-md hover:bg-slate-800 transition" %>
                  <%= link_to t("layout.nav.clients"), admin_users_path, class: "text-slate-200 hover:text-white px-2 py-1 rounded-md hover:bg-slate-800 transition" %>
                <% else %>
                  <%= link_to t("layout.nav.my_appointments"), appointments_path, class: "text-slate-200 hover:text-white px-2 py-1 rounded-md hover:bg-slate-800 transition" %>
                  <%= link_to t("layout.nav.request_appointment"), new_appointment_path, class: "text-slate-200 hover:text-white px-2 py-1 rounded-md hover:bg-slate-800 transition" %>
                <% end %>
              </div>
            </div>
            <div class="hidden md:flex items-center gap-4">
              <div class="inline-flex items-center rounded-full bg-slate-800 px-1 py-0.5 text-[10px]">
                <% current = I18n.locale.to_s %>
                <%= button_to t("layout.nav.language.en"),
                              locale_path,
                              method: :post,
                              params: { locale: "en" },
                              class: "inline-flex items-center rounded-full px-2 py-0.5 font-semibold #{current == 'en' ? 'bg-white text-slate-900' : 'text-slate-200 hover:bg-slate-700 hover:text-white'}" %>
                <%= button_to t("layout.nav.language.pt"),
                              locale_path,
                              method: :post,
                              params: { locale: 'pt-BR' },
                              class: "ml-1 inline-flex items-center rounded-full px-2 py-0.5 font-semibold #{current == 'pt-BR' ? 'bg-white text-slate-900' : 'text-slate-200 hover:bg-slate-700 hover:text-white'}" %>
              </div>
              <span class="text-xs text-slate-300"><%= current_user.email %></span>
              <%= button_to t("layout.nav.logout"),
                            destroy_user_session_path,
                            method: :delete,
                            data: { turbo: false },
                            class: "inline-flex items-center rounded-md bg-red-500 px-3 py-1.5 text-xs font-semibold text-white shadow-sm hover:bg-red-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-500" %>
            </div>
            <div class="flex items-center md:hidden">
              <button type="button"
                      class="inline-flex items-center justify-center rounded-md p-1.5 text-slate-200 hover:bg-slate-800 hover:text-white focus:outline-none focus:ring-2 focus:ring-white"
                      @click="open = !open"
                      aria-label="Toggle navigation">
                <svg x-show="!open" class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
                <svg x-show="open" x-cloak class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>
          </div>
          <div class="md:hidden" x-show="open" x-transition>
            <div class="space-y-1 pb-3 pt-2">
              <% if current_user.admin? %>
                <%= link_to "All appointments", admin_appointments_path, class: "block rounded-md px-3 py-2 text-sm font-medium text-slate-100 hover:bg-slate-800" %>
                <%= link_to "Clients", admin_users_path, class: "block rounded-md px-3 py-2 text-sm font-medium text-slate-100 hover:bg-slate-800" %>
              <% else %>
                <%= link_to "My appointments", appointments_path, class: "block rounded-md px-3 py-2 text-sm font-medium text-slate-100 hover:bg-slate-800" %>
                <%= link_to "Request appointment", new_appointment_path, class: "block rounded-md px-3 py-2 text-sm font-medium text-slate-100 hover:bg-slate-800" %>
              <% end %>
              <div class="border-t border-slate-800 mt-2 pt-2 flex items-center justify-between px-3 pb-2">
                <div class="flex items-center gap-2">
                  <% current = I18n.locale.to_s %>
                  <div class="inline-flex items-center rounded-full bg-slate-800 px-1 py-0.5 text-[10px]">
                    <%= button_to t("layout.nav.language.en"),
                                  locale_path,
                                  method: :post,
                                  params: { locale: "en" },
                                  class: "inline-flex items-center rounded-full px-2 py-0.5 font-semibold #{current == 'en' ? 'bg-white text-slate-900' : 'text-slate-200 hover:bg-slate-700 hover:text-white'}" %>
                    <%= button_to t("layout.nav.language.pt"),
                                  locale_path,
                                  method: :post,
                                  params: { locale: 'pt-BR' },
                                  class: "ml-1 inline-flex items-center rounded-full px-2 py-0.5 font-semibold #{current == 'pt-BR' ? 'bg-white text-slate-900' : 'text-slate-200 hover:bg-slate-700 hover:text-white'}" %>
                  </div>
                  <span class="text-xs text-slate-400 truncate"><%= current_user.email %></span>
                </div>
                <%= button_to t("layout.nav.logout"),
                              destroy_user_session_path,
                              method: :delete,
                              data: { turbo: false },
                              class: "inline-flex items-center rounded-md bg-red-500 px-2.5 py-1 text-xs font-semibold text-white hover:bg-red-600" %>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <% if notice.present? || alert.present? %>
      <div class="mx-auto mt-4 max-w-4xl px-4 space-y-2">
        <% if notice.present? %>
          <div x-data="{ show: true }"
               x-show="show"
               x-transition
               class="flex items-start justify-between rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800">
            <span><%= notice %></span>
            <button type="button"
                    class="ml-3 text-emerald-700 hover:text-emerald-900"
                    @click="show = false">
              &times;
            </button>
          </div>
        <% end %>
        <% if alert.present? %>
          <div x-data="{ show: true }"
               x-show="show"
               x-transition
               class="flex items-start justify-between rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800">
            <span><%= alert %></span>
            <button type="button"
                    class="ml-3 text-red-700 hover:text-red-900"
                    @click="show = false">
              &times;
            </button>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="mx-auto max-w-4xl px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>
