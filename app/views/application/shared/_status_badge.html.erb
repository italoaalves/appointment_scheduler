<span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium <%= status_badge_classes(status) %>">
  <%= local_assigns[:label].presence || (status.present? ? t("activerecord.attributes.appointment.status.#{status}") : "—") %>
</span>
