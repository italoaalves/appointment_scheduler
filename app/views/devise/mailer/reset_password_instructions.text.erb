Hello <%= @resource.email %>.

Someone has requested a link to change your password. Use the secure link below:
<%= edit_password_url(@resource, reset_password_token: @token) %>

If you did not request this, you can ignore this email. Your password will stay the same until you use the link above and create a new one.
