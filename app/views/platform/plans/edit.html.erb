<div class="space-y-6">
  <%= render "shared/page_header",
             title:    t("platform.plans.edit.title"),
             subtitle: t("platform.plans.edit.subtitle", name: @plan.name) %>

  <%= render layout: "shared/card" do %>
    <%= render "form", plan: @plan %>
  <% end %>
</div>
