<div class="space-y-6">
  <% page_actions = capture do %>
    <%= link_to t("platform.space_appointments.index.customers"), platform_space_customers_path(@space), class: button_classes(:secondary) %>
    <%= link_to t("platform.space_appointments.index.back_to_space"), platform_space_path(@space), class: button_classes(:secondary) %>
  <% end %>
  <%= render "application/shared/page_header", title: t("platform.space_appointments.index.title", space: @space.name), actions: page_actions %>

  <%= form_with url: platform_space_appointments_path(@space),
                method: :get,
                local: true,
                class: "flex flex-wrap items-end gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm" do %>
    <div class="flex flex-col gap-1">
      <label for="status" class="<%= form_label_classes_sm %>"><%= t("platform.appointments.filters.status") %></label>
      <%= select_tag :status,
                     options_for_select(
                       [
                         [ t("platform.appointments.filters.all_statuses"), "" ],
                         [ t("platform.appointments.filters.pending"), "pending" ],
                         [ t("platform.appointments.filters.confirmed"), "confirmed" ],
                         [ t("platform.appointments.filters.cancelled"), "cancelled" ],
                         [ t("platform.appointments.filters.rescheduled"), "rescheduled" ],
                         [ t("platform.appointments.filters.no_show"), "no_show" ],
                         [ t("platform.appointments.filters.finished"), "finished" ]
                       ],
                       params[:status]
                     ),
                     class: "rounded-md border-slate-300 text-sm focus:border-slate-500 focus:ring-slate-500" %>
    </div>
    <div class="flex flex-col gap-1">
      <label for="date_from" class="<%= form_label_classes_sm %>"><%= t("platform.appointments.filters.from") %></label>
      <%= date_field_tag :date_from, params[:date_from], class: "rounded-md border-slate-300 text-sm focus:border-slate-500 focus:ring-slate-500" %>
    </div>
    <div class="flex flex-col gap-1">
      <label for="date_to" class="<%= form_label_classes_sm %>"><%= t("platform.appointments.filters.to") %></label>
      <%= date_field_tag :date_to, params[:date_to], class: "rounded-md border-slate-300 text-sm focus:border-slate-500 focus:ring-slate-500" %>
    </div>
    <div class="flex gap-2">
      <%= submit_tag t("platform.appointments.filters.apply"), class: button_classes(:primary) %>
      <%= link_to t("platform.appointments.filters.clear"), platform_space_appointments_path(@space), class: button_classes(:secondary) %>
    </div>
  <% end %>

  <% if @appointments.any? %>
    <div class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
      <table class="min-w-full divide-y divide-slate-200 text-sm">
        <thead class="bg-slate-50">
          <tr>
            <th scope="col" class="px-4 py-2 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("platform.appointments.index.table.customer") %>
            </th>
            <th scope="col" class="px-4 py-2 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("platform.appointments.index.table.scheduled") %>
            </th>
            <th scope="col" class="px-4 py-2 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("platform.appointments.index.table.status") %>
            </th>
            <th scope="col" class="px-4 py-2 text-right text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("platform.appointments.index.table.actions") %>
            </th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-200">
          <% @appointments.each do |appointment| %>
            <tr class="hover:bg-slate-50">
              <td class="px-4 py-2 align-top text-slate-900">
                <%= appointment.customer&.name || "—" %>
              </td>
              <td class="px-4 py-2 align-top text-slate-700">
                <%= format_appointment_time(appointment.scheduled_at, space: appointment.space, format: :short) || "—" %>
              </td>
              <td class="px-4 py-2 align-top">
                <%= render "application/shared/status_badge", status: appointment.status %>
              </td>
              <td class="px-4 py-2 align-top">
                <%= link_to t("platform.appointments.index.view"), platform_space_appointment_path(@space, appointment), class: button_classes(:table_link) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <%= paginate @appointments, params: { status: params[:status], date_from: params[:date_from], date_to: params[:date_to] } %>
  <% else %>
    <%= render "application/shared/empty_state", content: t("platform.appointments.index.empty") %>
  <% end %>
</div>
